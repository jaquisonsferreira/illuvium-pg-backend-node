# Force platform-specific packages to be included
force=true
legacy-peer-deps=true

# Ensure native dependencies are rebuilt
rebuild=true

# Platform-specific settings for deployment
platform=linux
arch=x64